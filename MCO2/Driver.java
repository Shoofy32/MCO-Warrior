

public class Driver {
    
    public static void main(String[] args){

        //CLIViewer cliView = new CLIViewer();
        //EventController event = new EventController(cliView, guiView); //Creates the event controller (Does most functions of the game)
        //Scanner input = new Scanner(System.in);


        //createAndStoreInstances(event); //Method to call to create object instances and store them for the game

        //event.start(input);

        //input.close(); //Closes scanner

        new GUIEventController();
            
    }

}
